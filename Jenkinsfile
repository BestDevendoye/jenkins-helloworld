node {
    // some block
    stage('clone') {
    // some block
    git 'https://github.com/BestDevendoye/jenkins-helloworld.git'
    }
        stage('build') {
    // some block
    sh 'javac Main.java'
    }
        stage('Run') {
    // some block
    sh 'java Main'
    }
}
