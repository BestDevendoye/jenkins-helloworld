node {
    // some block
	stage('clone') {
    // some block
	git 'https://github.com/BestDevendoye/jenkins-helloworld.git'
}
stage('build') {
    // some block
	powershell 'javac Main.java'
}

stage('run') {
    // some block
	powershell 'java Main'
}
}
